# FreshKart Local

A colourful Streamlit MVP for a hyperlocal vegetable marketplace.

## Run locally

```powershell
python -m pip install -r requirements.txt
streamlit run app/Home.py
```

The app includes a customer marketplace, cart and UPI/COD demo checkout, plus a lightweight vendor dashboard. The sample vendor's WhatsApp button opens a prefilled WhatsApp chat.
